#include <iostream>
#include <string>
using namespace std;

int main(){
    
    string text;
    while(cin>>text);
    cout<<text.length();
    return 0;
}

#c++可以用cin和getline捕获输入，其中cin会以空格分隔，刚好符合题目。while循环自动取出最后一个单词，输出长度即可。
